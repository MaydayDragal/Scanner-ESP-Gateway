#pragma once

/* Settings used by the scan request and all status views. */
enum {
    SCANNER_DPI = 300,
    SCANNER_JPEG_QUALITY = 75,
    SCANNER_CANVAS_WIDTH = SCANNER_DPI * 17 / 2,
    SCANNER_CANVAS_HEIGHT = SCANNER_DPI * 14,
};
